<resources>

    <!-- Base application theme. -->
    <style name="AppTheme"
        parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeNoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimaryDark">@color/actionbar_bg</item>
        <item name="colorPrimary">@color/actionbar_bg</item>
        <item name="android:windowBackground">@color/window_bg</item>
        <item name="actionMenuTextAppearance">@style/menuText</item>
        <item name="popupTheme">@style/popupTheme</item>
        <item name="android:windowAnimationStyle">@style/activityAnim</item>
    </style>


    <style name="menuText">
        <item name="android:textStyle">normal</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="popupTheme" parent="Theme.AppCompat.Light"></style>

    <style name="activityAnim" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_open_in_right</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_close_out_right</item>
        <item name="android:activityOpenExitAnimation">@anim/empty</item>
        <item name="android:activityCloseEnterAnimation">@anim/empty</item>
    </style>

    <style name="hintDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/shape_dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
